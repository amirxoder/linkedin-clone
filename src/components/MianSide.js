import React from "react";
import styled from "styled-components";

const Container = styled.div`
  grid-area: main;
`;

const MianSide = () => {
  return <Container>MianSide</Container>;
};

export default MianSide;
